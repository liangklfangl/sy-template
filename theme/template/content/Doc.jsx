import React from "react";
/**
 * [这个组件用于显示有demo的页面，像组件Components下都是有demo属性的，这时候可能有分成两列来展示Demo]
 * @param  {[type]} props [description]
 * @return {[type]}       [description]
 */
export default (props)=>{
    console.log("含有Demo的页面内容:",props);
    	return (
              <div>hello</div>
    		)
    
}